{
    "name": "openinlib",
    "version": "0.0.1",
    "description": "An open source library for Indian resources",
    "scripts": {
        "postcss:build": "postcss static/styles.pcss -o static/styles.css",
        "prestart": "npm run postcss:build",
        "prebuild": "npm run postcss:build -- --env=production"
    },
    "devDependencies": {
        "autoprefixer": "^10.3.1",
        "postcss": "^8.3.6",
        "tailwindcss": "^2.2.6"
    },
    "dependencies": {
        "@tailwindcss/forms": "^0.3.3",
        "postcss-cli": "^8.3.1"
    }
}